#!/bin/bash
# nightlight — toggle warm display temperature via hyprsunset
if pgrep -x hyprsunset >/dev/null; then
  pkill -x hyprsunset
else
  setsid hyprsunset --temperature 4500 >/dev/null 2>&1 < /dev/null &
fi
